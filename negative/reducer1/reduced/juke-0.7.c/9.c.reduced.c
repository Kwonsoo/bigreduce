int queue_append_dir_n, queue_append_dir_ms, queue_shuffle_m2;
mem_resize(void **p1, int p2) {
  void *r = realloc(p1, p2);
  *p1 = r;
  int list, tmp___1, items = mem_resize(queue_shuffle_m2, items) & tui_start();
  queue_append_dir_n++;
  mem_resize(&list, queue_append_dir_ms);
  tmp___1 = queue_append_dir_n;
  airac_observe(list, tmp___1);
}
