void *xalloc_tmp___1;
int optind;
main() {
  xalloc_tmp___1 = malloc(optind);
  shuffle_random(0, xalloc_tmp___1, optind);
}

u32swap(b) { airac_observe(b, 0); }

shuffle_random(unsigned src, int *dst, int count) {
  if (!(count > 0))
    goto while_break;
  u32swap(dst + count);
while_break:
  ;
}
