struct _List {
  void *data;
} main_arglist;
struct _HttpHeaderEntry {
  char;
} * url_resource_new_tmp;
list_append(struct _List *p1, void *p2) {
  p1->data = p2 = malloc(sizeof(struct _HttpHeaderEntry));
}

main() {
  int *rsrc;
  url_resource_new_tmp = malloc(sizeof(int));
  rsrc = url_resource_new_tmp;
  airac_observe(rsrc, 0);
  list_append(&main_arglist, rsrc);
  rsrc = main_arglist.data;
}
