long a;
int c[5];
char d;
int e, i, j, k, l;
long (*f)(int, int, unsigned, const, int, long, int, void *, double, double);
double g, h;
minter_ansi_ultracompact_1(int bits, int best, unsigned block, unsigned IV,
                           int tailIndex, long maxIter, int b, void *user_args,
                           double counter, double expected) {
  int t = 0;
  while (1) {
    if (!(t < 5))
      goto while_break___4;
    airac_observe(IV, t);
    t++;
  }
while_break___4:
  t = tailIndex - 5;
}

long *b = minter_ansi_ultracompact_1;
hashcash_select_minter() { a = *b; }

hashcash_fastmint_user_args() {
  f = &a;
  f(k, i, d, c, e, j, l, hashcash_fastmint_user_args, g, h);
}
