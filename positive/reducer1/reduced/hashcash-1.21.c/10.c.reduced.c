long a, d;
const b[5];
char e;
int f;
long (*g)(int, int, unsigned, const, int, long, int, void *, double, double);
minter_ansi_compact_1(int bits, int best, unsigned block, unsigned IV,
                      int tailIndex, long maxIter, int b, void *user_args,
                      double counter, double expected) {
  int t = 0;
  while (1) {
    if (!(t < 5))
      goto while_break___3;
    airac_observe(IV, t);
    t++;
  }
while_break___3:
  ;
}

long *c = minter_ansi_compact_1;
hashcash_select_minter() { a = *c; }

hashcash_per_sec_calc() {
  g = &a;
  g(4, f, e, b, 2, d, 0, 0, 0, 0);
}
