struct xram {
  struct xram *next;
  struct xram *ram[5];
  int;
  char *title;
  int;
};
struct n_deb {
  struct n_deb *next;
  char;
  const *argp_domain;
} * calloc2_tmp;
void *xref_pass_tmp___1;
calloc2(p1, p2) {
  calloc2_tmp = calloc(p1, p2);
  return calloc2_tmp;
}

main() {
  struct n_deb *n_deb;
  xref_pass_tmp___1 = calloc2(1, sizeof(struct n_deb));
  n_deb = xref_pass_tmp___1;
  airac_observe(n_deb, 0);
  calloc2(1, sizeof(struct xram));
}

parse_opt() { calloc2(1, sizeof(int)); }
