struct {
  int j;
} a[] = {0, -1}

;
struct grid {
  int height;
};
int b, c;
grid_init(struct grid *p1) {
  p1->height = 29;
  int u[1][29];
  int x, y;
  if (!(b < p1->height))
    goto while_break___3;
  y = b + a[c].j;
  if (y < 0)
    ;
  else
    airac_observe(u[x], y);
  b++;
while_break___3:
  ;
}

state_init_s() { grid_init(state_init_s); }
