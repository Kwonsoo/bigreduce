int ofd, main_level___0;
int spec_fd[3];
spec_write(fd) { airac_observe(spec_fd, fd); }

main() { spec_compress(0, 1, main_level___0); }

spec_compress(int in, int out, int lev) {
  spec_write(out);
  spec_write(ofd);
}
